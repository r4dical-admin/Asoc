# INC-1002 Chat - Eli

`12:07Z` Pulling mailbox access, device registration, and refresh token activity into one timeline.

`12:14Z` Revocation happened quickly, but the unmanaged workstation still needs memory capture before we close the loop.

`12:31Z` Adding the forensics request to the overview so the next shift understands why the background agent is still queued.
