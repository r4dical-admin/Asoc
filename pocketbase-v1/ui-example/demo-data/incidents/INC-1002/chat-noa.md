# INC-1002 Chat - Noa

`12:05Z` Reviewing the risky OAuth grant against the affected user's normal device history.

`12:12Z` The consented app name looks benign, but the redirect path matches the phishing proxy pattern from the KB note.

`12:28Z` I have asked Eli to correlate mailbox access after the grant and to confirm token revocation timing.
