# INC-1003 Chat - Tom

`05:46Z` Comparing the user agents in the request samples against prior scraping incidents.

`06:09Z` The token is scoped to read-only, but endpoint selection still exposes too much metadata density.

`08:40Z` IOC Enricher output lines up with the prior February incident; adding that cross-reference to the timeline.
